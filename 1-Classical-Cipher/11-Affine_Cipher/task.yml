name: 11 - Affine Cipher（必做）
category: 1 - Classical Cipher
description: |
  `cipher.txt` 裡面是一段被仿射密碼加密後的數據，把這段數據用 Python 解密後（不需要分析這段數據解密後在講什麼，理論上解密後會拿到一段是全大寫的數據）直接丟到 Crypto Playground 的 Get Flag 裡拿 flag

  注意仿射密碼的 charset 是 `ABCDEFGHIJKLMNOPQRSTUVWXYZ ,.`
author: Curious
connection_info: http://{server}:8000/get-flag
flag: FLAG{510fc8a1bf88260c1a1f24491927d5419fadafafc3764aaba07900cf9f4b397a}
distfiles:
  - ./dist

type: standard
value: 100
state: visible

canonical_name: classical-cipher-substitution
