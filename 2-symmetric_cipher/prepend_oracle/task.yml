name: Prepend Oracle
category: 2 - Symmetric Cipher
description: |
  首先先理解 `server.py` 在做什麼，假設 `FLAG` 是 `SUS`，嘗試用 Crypto Playground 的 AES ECB Mode 然後手動把這三個字串打出來

  另外這題的 Flag 是 `FLAG{SUS}`
author: Curious
connection_info: http://{ip}:50000/aes/aes-ecb-mode
flag: FLAG{SUS}
distfiles:
  - ./chal
  - ./dist

type: standard
value: 500
state: visible

canonical_name: symmetric-cipher-prepend-oracle
